<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title ? (title + " • Naresh Pizza Eats") : "Naresh Pizza Eats" %></title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
<header class="site-header">
  <nav class="nav">
    <a class="logo" href="/">🍕 Naresh Pizza Eats</a>
    <ul class="nav-links">
      <li><a href="/">Menu</a></li>
      <li><a href="/contact">Contact</a></li>
      <% if (user) { %>
        <li class="muted">Hi, <%= user.name %></li>
        <li><a href="/logout">Logout</a></li>
      <% } else { %>
        <li><a href="/login">Login</a></li>
        <li><a href="/signup">Signup</a></li>
      <% } %>
      <li><a class="cart-link" href="/cart">Cart (<%= typeof cartQty !== 'undefined' ? cartQty : 0 %>)</a></li>
      <% if (user && user.is_admin) { %>
        <li><a class="admin-link" href="/admin">Admin</a></li>
      <% } %>
    </ul>
  </nav>
</header>

<main class="container">
  <% if (flash) { %>
    <div class="flash <%= flash.type %>"><%= flash.msg %></div>
  <% } %>